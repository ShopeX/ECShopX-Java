<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shopex.ecshopx.distribution.mapper.DistributorSalesCountMapper">

	<select id="selectTotalSalesCountByDistributor" resultType="java.lang.Long">
		SELECT COALESCE(SUM(order_item_count), 0)
		FROM distribution_distributor_sales_count
		WHERE company_id = #{companyId}
		AND distributor_id = #{distributorId}
	</select>
</mapper>
