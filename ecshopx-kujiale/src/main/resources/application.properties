# Kujiale H5 cities.json outbound HTTPS (RestTemplate + JDK URLConnection).
# When true, TLS certificate verification is disabled (insecure). Use only in dev/test; never in production.
ecshopx.kujiale.cities-json.insecure-tls=false
