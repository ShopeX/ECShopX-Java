<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shopex.ecshopx.reservation.mapper.ResourceLevelRelServiceMapper">

	<select id="selectListForReservationCheck" resultType="cn.shopex.ecshopx.reservation.domain.ResourceLevelRelService">
		SELECT resource_level_id, company_id, shop_id, material_id, created, updated
		FROM reservation_level_rel_service
		WHERE company_id = #{companyId}
		AND shop_id = #{shopId}
		AND material_id = CAST(#{labelId} AS UNSIGNED)
	</select>
</mapper>
