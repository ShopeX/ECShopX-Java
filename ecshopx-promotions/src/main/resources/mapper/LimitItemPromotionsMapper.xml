<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shopex.ecshopx.promotions.mapper.LimitItemPromotionsMapper">

	<select id="selectActiveRowsForTagActivityCheck"
			resultType="cn.shopex.ecshopx.promotions.dto.LimitItemTagCheckRow">
		SELECT limit_id AS limitId, item_id AS itemId, item_type AS itemType, start_time AS startTime, end_time AS endTime
		FROM promotions_limit_item
		WHERE company_id = #{companyId}
		AND end_time &gt;= #{now}
		AND (
		<trim prefixOverrides="OR">
			<if test="tagIds != null and tagIds.size() &gt; 0">
				OR (item_type = 'tag' AND item_id IN
				<foreach collection="tagIds" item="id" open="(" separator="," close=")">
					#{id}
				</foreach>
				)
			</if>
			<if test="requestItemIds != null and requestItemIds.size() &gt; 0">
				OR (item_type = 'normal' AND item_id IN
				<foreach collection="requestItemIds" item="id" open="(" separator="," close=")">
					#{id}
				</foreach>
				)
			</if>
			<if test="brandIds != null and brandIds.size() &gt; 0">
				OR (item_type = 'brand' AND item_id IN
				<foreach collection="brandIds" item="id" open="(" separator="," close=")">
					#{id}
				</foreach>
				)
			</if>
			<if test="categoryIds != null and categoryIds.size() &gt; 0">
				OR (item_type = 'category' AND item_id IN
				<foreach collection="categoryIds" item="id" open="(" separator="," close=")">
					#{id}
				</foreach>
				)
			</if>
		</trim>
		)
	</select>

	<select id="selectWxappActiveLimitItemsForDetail" resultType="cn.shopex.ecshopx.promotions.domain.LimitItemPromotions">
		SELECT *
		FROM promotions_limit_item
		WHERE company_id = #{companyId}
		  AND item_type = 'normal'
		  AND start_time &lt; #{now}
		  AND end_time &gt; #{now}
		  AND distributor_id IN (0, #{distributorId})
		  AND item_id IN
		<foreach collection="itemIds" item="iid" open="(" separator="," close=")">
			#{iid}
		</foreach>
		ORDER BY item_id ASC
	</select>

	<select id="selectWxappActiveLimitItemsForDetailByType" resultType="cn.shopex.ecshopx.promotions.domain.LimitItemPromotions">
		SELECT *
		FROM promotions_limit_item
		WHERE company_id = #{companyId}
		  AND item_type = #{itemType}
		  AND start_time &lt; #{now}
		  AND end_time &gt; #{now}
		  AND distributor_id IN (0, #{distributorId})
		  AND item_id IN
		<foreach collection="itemIds" item="iid" open="(" separator="," close=")">
			#{iid}
		</foreach>
		ORDER BY item_id ASC
	</select>
</mapper>
