<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shopex.ecshopx.deposit.mapper.DepositTradeMapper">

	<select id="sumRechargeSuccessFenByUserIds" resultType="map">
		SELECT user_id, SUM(money) AS money_sum
		FROM deposit_trade
		WHERE user_id IN
		<foreach collection="userIds" item="uid" open="(" separator="," close=")">#{uid}</foreach>
		  AND trade_status = 'SUCCESS'
		  AND trade_type = 'recharge'
		GROUP BY user_id
	</select>
</mapper>
